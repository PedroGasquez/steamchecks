public enum AlertType
{
  /// <summary>Dispara quando o preço do item for menor ou igual ao valor definido</summary>
  BelowPrice = 1,

    /// <summary>Dispara quando o preço do item for maior ou igual ao valor definido</summary>
    PercentPrice = 2
}